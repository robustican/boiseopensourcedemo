============
Installation
============

At the command line::

    $ easy_install boiseopensourcedemo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv boiseopensourcedemo
    $ pip install boiseopensourcedemo